export function ExpenseCard(expense) {
    // Formata valor para BRL (opcional, mas bom para UX)


    // Retorna o HTML preenchido com os dados
    return `
        <div class="">
          
        </div>
    `;
}